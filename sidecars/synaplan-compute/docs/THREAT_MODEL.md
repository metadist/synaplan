# Threat model (A0 / CP3, updated with A2 controls)

This expands master-plan §4.3. Every row names a proving test or corpus script that exists in this repository. A row without proof is not done; a claim the code does not implement is listed under *Not implemented*.

The sentence this track exists to change (`PlatformCapabilityInventory` `KNOWN_ABSENT` id `code_execution`):

> The assistant cannot execute Python, shell, or other code on the server

The sandbox answers that by executing **only** inside an ephemeral T1 (or T2) container, with no Synaplan credentials inside the room, push-in / pull-out files, and an API that is not a shell.

Attacker positions: **script author** (model-written code), **prompt injector** (untrusted chat text that becomes a script), **compromised compute host**, **malicious artefact** (poisoned `/out` file).

## Network posture (A0–A2)

**Egress is not implemented.** There is no proxy listener, `runner.Spec` has no network field, `Hardened` always emits `NetworkMode=none`, and `ValidateHardened` rejects anything else (including `container:`, `host`, `bridge`, and custom networks) before `ContainerCreate`. `POST /v1/runs` refuses any non-empty `egress.allow` with `egress_not_allowed` regardless of `COMPUTE_EGRESS_ENABLED`, and `GET /v1/health` reports `features.egress=false`. The allow-list policy (`egress.CheckAllowList`, `egress.Proxy`) is kept under test as the future contract only.

## Controls

| Threat | Attacker | Asset | T1 control | T2 control | Residual risk | Proof |
| ------ | -------- | ----- | ---------- | ---------- | ------------- | ----- |
| Container escape | script author | host kernel, other tenants | `Hardened`: NetworkMode none, ReadonlyRootfs, tmpfs `noexec,nosuid,nodev`, CapDrop ALL, no CapAdd, no-new-privileges, non-root uid:gid (`COMPUTE_SANDBOX_UID`, default 65534), Init, never privileged, empty `Env`, default pid/ipc/uts/userns/cgroupns, no devices, no docker.sock, no legacy `Binds`; bind sources must exist, be directories, and not be symlinks; `BindOptions.NonRecursive`; `ValidateHardened` re-checks every item at `Create` | gVisor `runsc` when `COMPUTE_TIER=gvisor`; Cloud requires T2 on a separate node | Kernel 0-days; accepted on T1, mitigated by T2/T3 | `TestHostConfigHardening`; `TestValidateHardenedRejectsWeakenedConfig`; `TestHardenedNeverPrivilegedEvenIfRuntimeSet`; corpus `setuid.py` / `setuid.js` |
| Data exfiltration via network | script author / prompt injector | user files in `/work` | Every run is `NetworkMode=none`; any `egress.allow` entry is refused; runner cannot select another mode | Same; runtime does not add a NIC | None until an egress proxy ships (separate review) | `TestEgressEmptyMeansNoNetwork`; `TestEgressFailsClosedEvenWhenEnabled`; `TestEgressAllowListRefusedFailClosed`; `TestValidateHardenedRejectsWeakenedConfig` (network cases); corpus `dns_attempt.py` / `dns_attempt.js` |
| Future egress to an unpinned or private IP | prompt injector | internal network | Policy kept for the proxy: pinned IPs only; loopback, private, link-local, multicast, CGNAT `100.64/10`, `0/8`, `192.0.0/24`, `198.18/15`, `255.255.255.255` refused; host cap | Same | Not wired to any container yet | `TestProxyRefusesUnpinnedHost`; `TestProxyRefusesPrivateIp`; `TestEgressDisabledRefusesAllowList` |
| Fork bomb / PID exhaustion | script author | host scheduler | `PidsLimit` from `limits.pids`; Init reaps the tree; `ValidateHardened` requires PidsLimit > 0 | gVisor extra | Shared-host noisy neighbour on T1; the service reports `program_error`, not `pids_limit` | `TestHostConfigHardening` (PidsLimit); `TestValidateHardenedRejectsWeakenedConfig` (pids cases); corpus `fork_bomb.py` / `fork_bomb.js` |
| Disk fill | script author | host disk | `fsize` ulimit = `outputMb` per file; tmpfs `size=64m`; after the run the sum of `/work` + `/out` above `outputMb` fails the run with `output_limit` and blocks artefact download; workspace quota is pre-checked on upload | Same | No in-run filesystem quota: a script can exceed `outputMb` until it exits; scratch volume sizing is an operator task | `TestOutputLimitAfterRunBlocksArtefacts`; `TestArtefactSizeCap`; `TestWorkspaceQuotaKillsRun`; `TestWorkspaceQuotaPrecheck`; corpus `disk_fill.py` / `disk_fill.js` |
| Infinite loop / hang | script author | capacity slots | `context.WithTimeout(limits.timeoutSec)`; on expiry SIGKILL + `ContainerRemove{Force}` with a fresh cleanup context; reason `timeout` | Same | Slow disk on kill | `TestTimeoutKillsAndReportsTimeout`; corpus `long_sleep.py` / `long_sleep.js` |
| Memory exhaustion | script author | host memory | `Memory` = `MemorySwap` = `limits.memoryMb`; `State.OOMKilled` → reason `oom` | Same | None material | `TestHostConfigHardening` (Memory/MemorySwap); `TestOOMKilledMapsToOOM`; `TestExit137WithoutOOMIsProgramError` |
| `/proc` walk / host environ | script author | host secrets | Namespaced proc; non-root; default PidMode | gVisor | Timing side channels (below) | `TestValidateHardenedRejectsWeakenedConfig` (host pid); corpus `proc_walk.py` / `proc_walk.js` |
| Symlink artefact or workspace entry pointing at host files | malicious artefact / script author | `/etc/passwd`, sibling scratch, other workspaces | `safepath`: root and every component opened with `O_NOFOLLOW` (`openat` for nested names) so a raced directory replace cannot walk out; listing never follows; a symlinked `/out` itself is refused | Same | None material on T1 | `TestOpenFileRefusesSymlinkedDirectory`; `TestOpenFileRefusesSymlinkedFile`; `TestOpenFileRefusesSymlinkRoot`; `TestArtefactSymlinkOmitted`; `TestArtefactSymlinkedOutDirRefused`; `TestWorkspaceRefusesSymlinkEscape`; corpus `symlink_out.py` / `symlink_out.js` |
| Poisoned MIME / huge artefact | malicious artefact | PHP ingest, browser preview | MIME allow-list and size cap on **both** list and download; `Content-Type` forced, `X-Content-Type-Options: nosniff`, `Content-Disposition: attachment`; workspace downloads apply the same list | Same | MIME is extension-first: a mislabelled file passes the list but is still served as that type with nosniff | `TestArtefactMimeRejected`; `TestArtefactOpenRefusesRejected`; `TestArtefactDownloadHeadersAndRefusals`; `TestWorkspaceDownloadAppliesMimeAllowList` |
| Workspace metadata tampering from inside the sandbox | script author | `owner`, `quotaMb` of the mounted workspace | Metadata lives at `<root>/<id>.json`; only `<root>/<id>/data` is bind-mounted | Same | None | `TestMetadataLivesOutsideMountedTree` |
| Log injection through stdout | script author | operator logs, PHP UI | stdout/stderr captured via `ContainerLogs` + `stdcopy`; server-side truncation at `COMPUTE_LOG_CAP_BYTES`; UTF-8 sanitize; audit log has no stdout field | Same | UI must still escape (Phase B) | `TestLogTruncation`; `TestLogTruncationIsReported`; `TestLogsCapturedAndStreamedAsSSE`; `TestAuditNeverLogsOutput`; corpus `huge_stdout.py` / `huge_stdout.js` |
| Cross-user workspace mount | prompt injector / buggy PHP | other user's files | Opaque ULID ids; owner match; path never in API; mount only on owner match; `ValidateHardened` requires the `/workspace` source under the workspaces root | Same | Compromised PHP sending another user's id — compute still 403s if owner mismatches | `TestRunWithForeignWorkspaceRefused`; `TestWorkspaceListNeverReturnsPaths`; `TestCreateOpaqueULIDAndOwner` |
| Run without owner / free image / free program | buggy PHP | host images, supply chain | Owner required; image is a map key; program allow-list (`python`/`sh`, `node`/`sh`) | Same | None | `TestRunRejectsMissingOwner`; `TestRunRejectsUnknownImage`; `TestRunRejectsProgramNotInAllowList` |
| Limits above instance caps | buggy PHP / DoS | host memory | Caps from health; `limits_exceed_caps` with `details {field, requested, cap}` | Same | None | `TestRunRejectsLimitsAboveCaps`; `TestLimitsErrorCarriesDetails` |
| Oversized or malformed request body | any caller | service memory, disk | `http.MaxBytesReader(COMPUTE_MAX_REQUEST_BYTES)` on every body; multipart parts bounded by `COMPUTE_MAX_FILES`; only declared parts accepted; `413 payload_too_large` | Same | None | `TestRequestBodyLimits`; `TestUndeclaredMultipartPartRejected`; `TestDecodeErrorCodes` |
| Compute API denial of service | unauthenticated caller | capacity | Bearer `COMPUTE_AUTH_TOKEN` ≥ 32 bytes compared as SHA-256 digests in constant time; health unauthenticated; buffered-channel semaphore of `COMPUTE_MAX_CONCURRENT` acquired before `ContainerCreate`; queue bounded by `COMPUTE_QUEUE_MAX`; `429 capacity_exceeded` | Same | Stolen token = full compute API (token grants nothing on Synaplan) | `TestUnauthenticatedRunsRejected`; `TestHealthIsPublic`; `TestNewRejectsShortToken`; `TestAllowConstantTimeMatch`; `TestAllowRejectsWrongLengthTokens`; `TestConcurrencyBoundIsEnforced`; `TestQueueFullReturns429` |
| Cancelled run keeps running or resurrects | operator / PHP | capacity, user data | Per-run `context.CancelFunc`; a queued run never creates a container after DELETE; running container killed; terminal status set once (`finish` idempotent) so `cancelled` never flips; scratch removed only after the run goroutine exits | Same | None | `TestDeleteRunningRunStaysCancelled`; `TestDeleteQueuedRunNeverCreatesContainer` |
| Scratch and status records accumulate | time | host disk, memory | Janitor prunes finished runs after `COMPUTE_RUN_RETENTION_MIN` and deletes scratch; `SweepOrphans` removes leftover containers **and** every scratch dir at startup | Same | Retention window itself | `TestJanitorPrunesFinishedRuns`; `TestEnsureScratchAndSweep` |
| Silent T1 where T2 was configured | misconfig | Cloud posture (row 14) | `COMPUTE_TIER=gvisor` fails startup without `runsc` | Forced gVisor | Operator not setting the env | `TestForcedTierFailsStartupWithoutRuntime`; `TestForcedMicroVMNeedsNamedRuntime` |
| Malformed configuration | misconfig | every control above | Any malformed `COMPUTE_*` numeric/boolean fails startup; caps must be positive; sandbox uid/gid must be non-root | Same | Wrong-but-valid values | `TestLoadFailsOnMalformedValues`; `TestLoadRejectsShortToken` |
| Timing side channels between concurrent runs | script author | co-tenant secrets | Accepted on T1 (shared kernel) | Mitigated by T2/T3 and a separate compute node for Cloud | Cache/timing leakage on T1 self-host | Documented residual; Cloud requires T2 |
| Image supply chain | compromised registry | every run | Digest-pinned map; a run never pulls; `RequireDigests` | Cosign in A3 | Placeholder digests until A3 publishes | `TestImageMapRequiresDigest` |
| Compute service compromise | compromised compute host | Synaplan credentials, user PII | Service holds only the bearer token; no user emails, no file paths, no Synaplan DB; not published to the internet | Separate node | Attacker can run more sandboxes as that token | `TestAuditNeverLogsOutput` (audit field list) |
| Prompt-injected code | prompt injector | user files selected for the run | Policy (track 4) decides; sandbox limits blast radius; audit `run.accepted` / `run.finished` with image, limits, reason, bytes — no stdout | Same | User-selected files are in `/work` by design | `TestAuditRefusedRunHasReason`; policy in Phase B |
| API shell string | prompt injector | host via compute process | No free-form shell in the contract; `entry.program` + `args[]`; `scripts/no-shell-guard.sh` | Same | `sh` as an in-sandbox program is allowed (decision 3) | `scripts/no-shell-guard.sh`; `TestUnknownFieldRejected` |

## Known limitations

- **Bind mounts are executable.** The Docker Engine API (`mount.BindOptions` in SDK v23) has no `noexec` / `nosuid` / `nodev` option for bind mounts; only tmpfs takes them. `/work`, `/out`, and `/workspace` are therefore mounted `rw` with `NonRecursive` only. A script can `chmod +x` and execute a file it wrote — no privilege is gained (CapDrop ALL, no-new-privileges, non-root) but it is not blocked. `/tmp` is `noexec,nosuid,nodev`.
- **No in-run filesystem quota.** `outputMb` is enforced per file by the `fsize` ulimit and as a total only after the container exits. The workspace quota is checked before the run from the upload size, not while the script writes.
- **`pids_limit` is never reported.** The cgroup refuses the fork; the script exits non-zero and the reason is `program_error`.
- **Permissions fall back to world-writable.** The service runs as distroless `nonroot`. Successful chown sets owner=`COMPUTE_SANDBOX_UID` and group=the service process gid (`0770`/`0660`) so the sandbox can write and the service can still list `/out`. When it lacks `CAP_CHOWN` it cannot do that, so it creates them `0777` / `0666` instead (logged once at startup). Keep `COMPUTE_SCRATCH_DIR` and `COMPUTE_WORKSPACES_DIR` on a volume no other local user can reach.
- **Usage accounting is partial.** `wallMs`, `bytesIn`, `bytesOut`, and the truncation flags are real; `cpuSec` and `maxMemoryMb` stay `0` until cgroup stats are read.

## Not implemented (do not claim)

- Egress of any kind: no proxy, no `compute-egress` network, no `HTTP_PROXY` injection.
- In-run workspace quota kill.
- Cosign verification of images (A3).
- Live hostile-corpus job: `tests/hostile` only verifies headers hermetically; execution against dockerd is gated on `COMPUTE_HOSTILE_DOCKER=1` and lives outside this repository's CI.
