# ==============================================================================
# Synaplan Screen Recording Configuration
# ==============================================================================
# Copy this file to .env and edit the values.
# .env is gitignored — never commit real credentials.

# ------------------------------------------------------------------------------
# 1. TARGET INSTANCE
# ------------------------------------------------------------------------------
# URL of the Synaplan instance to record against.
# For local development, use the Vite dev server URL.
SYNAPLAN_URL=https://demo.synaplan.com

# ------------------------------------------------------------------------------
# 2. DEMO CREDENTIALS
# ------------------------------------------------------------------------------
# Use a dedicated demo account — never real user credentials!
# Local dev fixtures: demo@synaplan.com / demo123 (see docs/DEVELOPMENT.md)
DEMO_EMAIL=demo@synaplan.com
DEMO_PASSWORD=demo

# ------------------------------------------------------------------------------
# 3. PACING
# ------------------------------------------------------------------------------
# STEP_DELAY: Playwright slowMo — milliseconds added between every low-level
# browser action (click, type, navigate). Makes the recording watchable.
STEP_DELAY=1500

# ACTION_PAUSE: Extra pause after scripted steps (login, send message, scroll).
# Controls the breathing room between major actions.
ACTION_PAUSE=2000

# ------------------------------------------------------------------------------
# 4. TTS NARRATION (optional)
# ------------------------------------------------------------------------------
# Narrated scenarios require:
#   - synaplan-tts running (docker compose up -d in synaplan-tts/ repo)
#   - ffmpeg installed on the host (brew install ffmpeg)
#
# If TTS is not available, silent scenarios (01-*, 02-*) still work fine.
# Only narrated scenarios (03-*) need these settings.

# TTS service URL (default port from synaplan-tts docker-compose)
TTS_URL=http://127.0.0.1:10200

# Voice to use for narration. Run: curl http://127.0.0.1:10200/api/voices
# Available: en_US-lessac-medium, de_DE-thorsten-medium, es_ES-davefx-medium,
#            tr_TR-dfki-medium, ru_RU-irina-medium, fa_IR-reza_ibrahim-medium
TTS_VOICE=en_US-lessac-medium

# Speech speed: 1.0 = normal, <1.0 = faster, >1.0 = slower
TTS_SPEED=1.0
