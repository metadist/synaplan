controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

# Plugin routes are loaded dynamically by Kernel::loadPluginRoutes()
# No manual plugin entries needed — drop plugin into /plugins/ and restart.
