# Git
.git
.gitignore
.gitattributes

# Documentation
README.md
LICENSE.md
_devextras/history.md

# IDE
.vscode
.idea
*.swp
*.swo
*~

# Backend excludes
backend/var/cache/*
backend/var/log/*
backend/vendor
backend/.env.local
backend/.env.docker

# Frontend excludes
frontend/node_modules
# Note: frontend/dist is NOT excluded because it's needed for the unified Docker image
# It contains the pre-built Vue.js app that gets copied into the backend container
# In development: contains only .gitkeep (minimal size)
# In CI: contains production build artifacts from the frontend job
frontend/.env.local
frontend/.env.docker

# Docker
docker-compose*.yml
# Note: _docker is needed for build context

# OS
.DS_Store
Thumbs.db

# Testing
backend/phpunit.xml
frontend/coverage

